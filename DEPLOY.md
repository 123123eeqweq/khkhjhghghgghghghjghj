# Инструкция по деплою

## 🚀 Фронтенд на Vercel

### 1. Подготовка

1. Убедитесь, что проект готов:
   ```bash
   npm run build
   ```

### 2. Деплой на Vercel

1. Зайдите на [vercel.com](https://vercel.com)
2. Импортируйте репозиторий (GitHub/GitLab/Bitbucket)
3. В настройках проекта добавьте **Environment Variables**:
   - `VITE_API_URL` = `https://your-railway-app.railway.app` (URL вашего бэкенда на Railway)

### 3. Build Settings

- **Framework Preset**: Vite
- **Build Command**: `npm run build`
- **Output Directory**: `dist`
- **Install Command**: `npm install`

---

## 🔧 Бэкенд на Railway

### 1. Подготовка

1. Убедитесь, что файл `server/package.json` содержит скрипт `start`

### 2. Деплой на Railway

1. Зайдите на [railway.app](https://railway.app)
2. Создайте новый проект
3. Выберите "Deploy from GitHub repo"
4. Выберите репозиторий
5. В настройках проекта:
   - **Root Directory**: установите `server`
   - **Start Command**: `npm start`

### 3. Environment Variables (Railway)

Добавьте следующие переменные окружения в Railway:

```
SUPABASE_URL=https://taoeikfqvyedgianjtmo.supabase.co
SUPABASE_KEY=ваш_service_role_ключ_из_Supabase
PASSWORD=1221
SESSION_DURATION=86400000
PORT=3001
FRONTEND_URL=https://your-vercel-app.vercel.app
```

**Важно**: 
- `SUPABASE_KEY` должен быть `service_role` ключом (не `anon`)
- Получить можно в Supabase Dashboard → Settings → API → service_role (secret)
- `FRONTEND_URL` - это URL вашего фронтенда на Vercel (добавьте после деплоя фронта)

**Важно**: 
- `SUPABASE_KEY` должен быть `service_role` ключом (не `anon`)
- Получить можно в Supabase Dashboard → Settings → API → service_role (secret)

### 4. После деплоя бэкенда

1. Railway даст вам URL вида: `https://your-app-name.railway.app`
2. Скопируйте этот URL
3. Вернитесь в настройки Railway и добавьте переменную `FRONTEND_URL` = URL вашего фронтенда на Vercel (после деплоя фронта)
4. Перезапустите сервис на Railway

### 5. После деплоя фронтенда

1. Vercel даст вам URL вида: `https://your-app.vercel.app`
2. В настройках Vercel добавьте переменную `VITE_API_URL` = URL вашего бэкенда на Railway (например: `https://your-app-name.railway.app`)
3. В настройках Railway обновите `FRONTEND_URL` на URL фронтенда
4. Передеплойте оба сервиса

---

## ✅ Проверка

1. Откройте URL вашего фронтенда на Vercel
2. Попробуйте войти с паролем `1221`
3. Проверьте, что всё работает

---

## 🔐 Безопасность

- **Никогда** не коммитьте `.env` файлы
- Используйте `service_role` ключ только на сервере
- В production используйте более сложный пароль

